#include <stdio.h>

int main()
{
    /*En el programa del ejercicio 1-6, si ingresamos -1 el resultado va a ser 1 , es decir EOF es diferente al
    -1 ingresado. Sin embargo, cuando ingresamos '-1' por teclado, lo que se pasa es una cadena de texto, no el
    numero -1, estos como cadena de texto y como numero tienen dos valores distintos.*/
    printf("El valor de EOF es %d:",EOF);
}